const popupContent = {
    textAlign: "center",
    height: "380px",
    marginTop: "30px"
  };
  const popupHead = {
    fontWeight: "bold",
    fontSize: "20px"
  };
  
  const popupText = {
    fontSize: "12px",
    marginBottom: "20px"
  };
  
  const okText = {
    fontSize: "20px"
  };
  
  export { popupContent, popupHead, popupText, okText };